#include <stdio.h>
#include <math.h>

int main() {
    float numero = 83; // Puedes cambiar este número por el que desees calcular su raíz cuadrada
    float raiz = sqrt(numero);

    printf("La raiz cuadrada de %.2f es %.2f\n", numero, raiz);

    return 0;
}
