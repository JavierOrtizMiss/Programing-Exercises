#include <stdio.h>
// Elaborar un programa que calcule todos los numeros de tres cifras tales que la suma de los cubos de las cifras es igual al valor del numero
int main() {
    int i, sum;
    sum=0;
    i=1;

    while (i<=10)
    {
        printf("%d \n", i);
        i+=1;
    }
    


    return 0;
}